import NavBar from "../../NavBar/NavBar";


const Header = () => {
    return (
        <header className="container mx-auto">
            <NavBar></NavBar>
        </header>
    );
};

export default Header;